# Alias: 4096 fine-tune is the C1 run. Kept so older docs still resolve.
run_name: c1_finetune_4096
init_checkpoint: /root/RL-2048/checkpoints/phaseA_dueling_seed0.pt
train_seed: 0
total_env_steps: 1000000
max_episode_steps: 5000
gamma: 0.995
lr: 0.00003
batch_size: 256
replay_capacity: 500000
learning_starts: 8000
train_freq: 4
target_update_freq: 1000
epsilon_start: 0.05
epsilon_end: 0.02
epsilon_decay_fraction: 0.5
grad_clip_norm: 10.0
use_double_dqn: true
huber_delta: 1.0
obs_scale: 16.0
obs_encoding: onehot
network_type: dueling_cnn
onehot_channels: 16
conv_channels: [256, 256, 256]
hidden_dims: [512]
symmetry_aug: true
n_step: 5
reward_mode: log1p
use_per: false
collect_decode: 2ply
collect_corner_tiebreak: true
collect_endgame_epsilon: 0.02
collect_endgame_tile: 1024
log_freq: 5000
eval_freq: 0
checkpoint_freq: 200000
output_dir: /root/autodl-tmp/RL-2048/results/runs/c1_finetune_4096
device: auto
num_eval_episodes: 200
eval_seed_set: val
num_envs: 1
