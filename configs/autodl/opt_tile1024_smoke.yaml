# Phase A smoke: same stack, 2M steps, dev 200 eval.
run_name: opt_tile1024_smoke
train_seed: 0
total_env_steps: 2000000
max_episode_steps: 1200
gamma: 0.995
lr: 0.0001
batch_size: 256
replay_capacity: 300000
learning_starts: 20000
train_freq: 4
target_update_freq: 1000
epsilon_start: 1.0
epsilon_end: 0.01
epsilon_decay_fraction: 0.8
grad_clip_norm: 10.0
use_double_dqn: true
huber_delta: 1.0
obs_scale: 16.0
obs_encoding: onehot
network_type: dueling_cnn
onehot_channels: 16
conv_channels: [256, 256, 256]
hidden_dims: [512]
symmetry_aug: true
n_step: 5
reward_mode: log1p
use_per: false
log_freq: 5000
eval_freq: 0
checkpoint_freq: 500000
output_dir: /root/autodl-tmp/RL-2048/results/runs/opt_tile1024_smoke
device: auto
num_eval_episodes: 200
eval_seed_set: dev
