# Conda environment (project-local prefix). Create with:
#   conda env create -f environment.yml -p .conda/env
# Activate with:
#   conda activate ./.conda/env

name: rl2048
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - -e ".[dev,train]"
